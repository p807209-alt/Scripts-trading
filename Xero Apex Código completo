task.spawn(function()
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")

-- 🔥 CAMBIO NOVA → XERO APEX (HOOK LIGERO)
local function hookLabel(v)
    if v:IsA("TextLabel") then
        v:GetPropertyChangedSignal("Text"):Connect(function()
            pcall(function()
                if v.Text and v.Text:find("Nova") then
                    v.Text = v.Text:gsub("Nova Hub", "Xero Apex")
                    v.Text = v.Text:gsub("Nova", "Xero Apex")
                end
            end)
        end)

        -- aplicar inicial
        pcall(function()
            if v.Text and v.Text:find("Nova") then
                v.Text = v.Text:gsub("Nova Hub", "Xero Apex")
                v.Text = v.Text:gsub("Nova", "Xero Apex")
            end
        end)
    end
end

-- 🔥 OCULTAR SOLO UN DISCORD (ULTRA LIGERO)

local TARGET = "discord.gg/vhu5rhjq9u"

local function hideIfMatch(obj)
    if obj:IsA("TextLabel") or obj:IsA("TextButton") then
        local t = obj.Text and string.lower(obj.Text)
        if t and t:find(TARGET) then
            obj.TextTransparency = 1
            obj.TextStrokeTransparency = 1
            obj.BackgroundTransparency = 1
        end
    end
end

-- 🔥 DETECTAR NUEVOS (SIN LAG)
game.DescendantAdded:Connect(function(obj)
    task.defer(function()
        pcall(function()
            hideIfMatch(obj)
            hookLabel(obj)
        end)
    end)
end)

-- 🔥 APLICAR A EXISTENTES
for _, v in ipairs(CoreGui:GetDescendants()) do
    pcall(function()
        hideIfMatch(v)
        hookLabel(v)
    end)
end

-- 🔥 EJECUTAR SCRIPT (PRIMERO)
loadstring(game:HttpGet("https://api.jnkie.com/api/v1/luascripts/public/752793106aac832ba8ddd9d8718e3189d73b69680acd7616ff256c77d42ac9d3/download"))()

-- 🔥 CREAR TU TEXTO (INSTANTE DESPUÉS)

local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "DiscordTextPro"
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

local text = Instance.new("TextLabel")
text.Name = "MY_DISCORD_SAFE"
text.Parent = gui

text.Size = UDim2.new(0, 300, 0, 18)
text.Position = UDim2.new(0.5, -150, 0.35, 0)

text.BackgroundTransparency = 1
text.Text = "discord.gg/GfbPeZrzhN"
text.TextScaled = true
text.Font = Enum.Font.GothamBold
text.TextColor3 = Color3.fromRGB(255, 255, 255)

local stroke = Instance.new("UIStroke", text)
stroke.Thickness = 1.5
stroke.Color = Color3.fromRGB(0, 0, 0)
stroke.Transparency = 0
end)
