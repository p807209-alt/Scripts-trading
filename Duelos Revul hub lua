local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Lista de UserIds permitidos
local WHITELIST = {
    [12345678] = true,
    [87654321] = true,
    [11223344] = true,
}

if not WHITELIST[player.UserId] then
    player:Kick("No autorizado")
    return
end

-- Ejecuta tu script si está permitido
loadstring(game:HttpGet("TU_LINK_AQUI"))()
