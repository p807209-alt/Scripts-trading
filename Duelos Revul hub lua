local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- 🔑 KEY CORRECTA
local CORRECT_KEY = "A3FCFB7ECC9B6D0A"

-- 👥 WHITELIST
local WHITELIST = {
    [12345678] = true,
    [87654321] = true,
}

-- 🔑 KEY DEL USUARIO
local userKey = getgenv().key

-- ❌ VALIDACIÓN
if not WHITELIST[player.UserId] or userKey ~= CORRECT_KEY then
    player:Kick("Revul Hub\n\nKey requerida /o No se encontro registro en el HWID")
    return
end

-- ✅ ACCESO → HUB
loadstring(game:HttpGet("https://gist.github.com/p807209-alt/a32ed744fa14d38c5455b9d21d138f53/raw")
